#include <stdio.h>

/**
 * sub - function that add two numbers
 * @a: first number
 * @b: second number
 * Return: the difference  of a and b
 */

int sub(int a, int b)
{       
        return (a - b);
}
